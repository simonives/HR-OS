.env
.DS_Store
.worktrees/
.superpowers/

# Local-only HR reference corpus: copyrighted book extracts used for
# grounding skill content during development (see CLAUDE.md). Never
# tracked, this repo is public and none of it is licensed for
# redistribution. Keep locally if useful, never commit.
resources/
ingestion_chamber/
**/*.epub
**/*.pdf
**/*.mobi
